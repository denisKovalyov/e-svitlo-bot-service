# e-svitlo-bot-service
Service which utilizes Telegram Bot API.

Communication with API should be configured by setting webhook: https://core.telegram.org/bots/webhooks

Architecture schema:

![](e-svitlo-bot-service.png)